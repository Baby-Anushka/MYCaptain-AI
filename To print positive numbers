#Number of elements in the list
n=int(input("number in a list"))
#Initialising list
lst = []
for i in range(0,n):
    e=int(input())
    lst.append(e)
#printing list
print(lst)
#Printing positive numbers
for num in lst:
    if num>0:
        print(num)
